# Keep the build context small and reproducible. node_modules and the
# built UI are produced inside the image; host-platform copies would be
# wrong (node-pty prebuilds are per-platform).
node_modules
**/node_modules
packages/ui/dist
artifacts
build
.git
.github
*.log
.DS_Store
