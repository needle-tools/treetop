{
  "name": "@supergit/daemon",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "bun --hot run src/server.ts",
    "start": "bash -c 'exec -a \"supergit prod\" bun run src/server.ts'",
    "test": "bun test"
  },
  "type": "module",
  "dependencies": {
    "bonjour-service": "^1.4.0",
    "node-pty": "^1.1.0",
    "sharp": "^0.34.5",
    "ssh2": "^1.16.0"
  }
}
