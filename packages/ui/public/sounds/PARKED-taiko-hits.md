## parked-taiko-hits.ogg

Multiple taiko hits — ~2s percussion burst. User likes it; no
assigned use yet.

### Candidate uses
- TBD — "I like it but don't know what for" (percussion).
- Possible homes worth considering when we wire it: heavy/successful
  commit, push completed (alt to git-push chime), build finished, big
  action confirmation, "boss-fight" moment for high-risk operations.

### Status
- Parked: not yet referenced in `sound.ts` `DEFAULT_MAPPINGS`.
- File: `parked-taiko-hits.ogg` (metadata stripped, libopus 96k).
