## parked-marimba-tremolo.ogg

African marimba chord with tremolo — ~8s ambient pad. User likes it;
no assigned use yet.

### Candidate uses
- TBD — "I like it but don't know what for" (warm musical pad).
- Worth considering for: ambient backdrop while a long operation
  runs (clone, big git fetch, attachment upload), arrival flourish
  for the first time a workspace loads in a session, "welcome back"
  cue when the daemon comes back online after a reconnect.

### Status
- Parked: not yet referenced in `sound.ts` `DEFAULT_MAPPINGS`.
- File: `parked-marimba-tremolo.ogg` (metadata stripped, libopus 96k).
